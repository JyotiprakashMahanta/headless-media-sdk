/// <reference path="../../../types/react-native.d.ts" />

/**
 * Pulls in the repo-level `react-native` typecheck shim. See
 * `types/react-native.d.ts` and the README for why RN is a peerDependency that
 * this repo does not install.
 */
