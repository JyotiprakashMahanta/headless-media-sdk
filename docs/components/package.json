{
  "name": "docs-components",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deployable documentation site for media-ui-react, with live demos. Depends on media-ui-react only — no SDK, no API key.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "media-ui-react": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.3",
    "vite": "^5.4.10"
  }
}
