{
  "name": "web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Demo app. The only package that imports both media-react (data) and media-ui-react (display).",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "media-react": "*",
    "media-ui-react": "*",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.3",
    "vite": "^5.4.10"
  }
}
